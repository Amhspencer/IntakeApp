<div class="btn-controls">
    <div class="btn-box-row row-fluid">
        <a href="#" class="btn-box big span4"><i class="fa fa-paste fa-lg"></i><b><%= @stat[:forms][:total_processed]%> / <%= @stat[:forms][:total_unprocessed]%> / <%= @stat[:forms][:total_form]%></b>
            <p class="text-muted">
                Processed / Unprocessed / Total</p>
        </a>
        <a href="#" class="btn-box big span4"><i class="fa fa-user fa-lg"></i><b><%= @stat[:actives][:total_active]%> / <%= @stat[:actives][:total_inactive]%></b>
            <p class="text-muted">
                Active / Inactive</p>
        </a>
        <a href="#" class="btn-box big span4"><i class="fa fa-group fa-lg"></i><b><%= @stat[:users][:total_admin]%> / <%= @stat[:users][:total_staff]%> / <%= @stat[:users][:total_partner]%></b>
            <p class="text-muted">
                Admins / Staff / Partners</p>
        </a>
    </div>
</div>
<!--/#btn-controls-->
